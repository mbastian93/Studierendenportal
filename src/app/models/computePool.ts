export class ComputePool {
  name: string;
  workplaces: number;
  printer: string;
  scanner: string;
  overhead: string;
  projector: string;
  audio: string;
  location: string;
  buildingNumber: number;
  imagepath: string;
}
