export class ComputePool {
  name: string;
  workplaces: number;
  printer: boolean;
  scanner: boolean;
  overhead: boolean;
  projector: boolean;
  audio: boolean;
  location: string;
  buildingNumber: number;
  imagepath: string;
}
